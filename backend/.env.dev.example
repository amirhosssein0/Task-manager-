# Django
DJANGO_SECRET_KEY=
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# CORS/CSRF (comma-separated)
CORS_ALLOWED_ORIGINS=http://localhost:3000
CSRF_TRUSTED_ORIGINS=http://localhost:3000

DJANGO_ENV=dev


# JWT Token Lifetimes
# Access token: can be set in minutes (ACCESS_TOKEN_MINUTES) or hours (ACCESS_TOKEN_HOURS)
# If ACCESS_TOKEN_MINUTES is set, it takes precedence
ACCESS_TOKEN_MINUTES=60
ACCESS_TOKEN_HOURS=1
# Refresh token: lifetime in days
REFRESH_TOKEN_DAYS=14

# Celery & Redis
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0

# Overdue Task Notification Time (24-hour format)
# Celery Beat will run flag_overdue_tasks at this time every day
OVERDUE_NOTIFY_HOUR=21
OVERDUE_NOTIFY_MINUTE=0

DB_NAME=task_manager
DB_USER=task_manager
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=5432

# Billing test card (backend validation)
TEST_CARD_NUMBER=4242424242424242
TEST_CARD_EXP=01/28
TEST_CARD_CVC=123

# SMTP Email Settings
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
DEFAULT_FROM_EMAIL=

# For development: use console backend to see emails in terminal
# EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend

# Timezone Configuration
DJANGO_TIME_ZONE=Asia/Tehran

# Recurring Tasks Creation Time (24-hour format, uses DJANGO_TIME_ZONE)
# Default: Midnight (00:00)
RECURRING_TASKS_HOUR=0
RECURRING_TASKS_MINUTE=0